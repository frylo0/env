" IMPORTANT!!!
" To instert <Esc> char to macros you need to press < Ctrl+V + Esc > inside of
" NeoVim editor. It will insert special char which records correctly.

" Wrap CSS property px value to em function
" width: 10px; => width: em(10px);
let @e='T iem(t;a)l'
