import { connect } from 'react-redux';
import { --Name-- } from './--Name--';
import {
   --Name--State,
} from './--name--Slice';

const mapStateToProps = (state: --Name--State) => ({
   
});

const mapDispatchToProps = {
   
};

export const --Name--Container = connect(mapStateToProps, mapDispatchToProps)(--Name--);
