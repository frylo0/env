import --name--Reducer, { --Name--State } from './--name--Slice';

describe('--name-- reducer', () => {
   const initialState: --Name--State = {

   };
   it('should handle initial state', () => {
      expect(--name--Reducer(undefined, { type: 'unknown' })).toEqual({
         
      });
   });
});
